# Neo4j connection (required for schema init, data loading, and relation import)
NEO4J_URI=neo4j+s://your-instance.databases.neo4j.io
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-password

# Gemini API key (only needed for integration tests)
# GEMINI_API_KEY=your-key
